# Experimental Features

This folder is where all experimental features are tested and iterated on before being promoted to the `features/` folder for production deployment.

Each subfolder should be a self-contained feature module that can be developed, tested, and reviewed in isolation before final deployment on the website.

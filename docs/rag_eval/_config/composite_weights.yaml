# Composite-score weights for the RAG eval framework.
# Per spec §3a, these are HASH-LOCKED at iter-01 and may not change mid-loop.
# Mid-loop drift is blocked by ops/scripts/lib/rag_eval_diff.py via composite.verify_weights_unchanged.
# Changing these requires a `decision` claude-mem observation explaining the rationale.

chunking: 0.10
retrieval: 0.25
reranking: 0.20
synthesis: 0.45

name: Deploy to DigitalOcean Droplet

# Canonical production target:
#   - DigitalOcean Premium Intel droplet (2 GB RAM, 1 vCPU, 70 GB NVMe SSD)
#   - Reserved IP attached (stable across droplet lifecycle events)
#   - Cloudflare DNS for apex zettelkasten.in
#   - Caddy 2 container terminates TLS (Let's Encrypt) and proxies to the active color
#   - Blue/green Docker Compose: blue=127.0.0.1:10000, green=127.0.0.1:10001
#   - Image registry: ghcr.io/chintanmehta21/zettelkasten-kg-website:<git-sha>
#   - Cutover script on droplet: /opt/zettelkasten/deploy/deploy.sh <git-sha>

on:
  push:
    branches: [master]
  workflow_dispatch:
    inputs:
      target_hostname:
        description: "Caddy hostname to deploy (zettelkasten.in or stage.zettelkasten.in)"
        required: false
        default: "zettelkasten.in"

concurrency:
  group: deploy-prod
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  test:
    name: pytest (mocked)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            ops/requirements.txt
            ops/requirements-dev.txt
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r ops/requirements-dev.txt
      - name: pytest
        env:
          GEMINI_API_KEY: "ci-stub"
          # Stubs for the v2 Supabase client so module-level / fallback
          # constructor paths don't fail validation under unit-test mocks.
          # Tests inject MagicMock clients — these values are never used to
          # make network calls; they only let get_v2_client() construct.
          SUPABASE_V2_URL: "https://ci-stub.supabase.co"
          SUPABASE_V2_ANON_KEY: "ci-stub-anon"
          SUPABASE_V2_SERVICE_ROLE_KEY: "ci-stub-service"
          NEXUS_TOKEN_ENCRYPTION_KEY: "7TgtMgeR5dMTnXxW6ULICwhf66A1VpzwuNFuIBqmoe4="
        run: pytest -q --maxfail=5

  # 2026-05-11: removed `migrations-manifest-check` job. It used to apply legacy
  # kg_public/migrations to vanilla postgres:15 and verify against a v1 manifest
  # that no longer exists. Re-pointing to _v2 won't work either: v2 SQL references
  # auth.users / auth.uid() / auth.jwt() which vanilla pgvector containers can't
  # provide. Coverage is preserved by:
  #   - .github/workflows/migration-ci.yml::migrate-v2-fresh — PR-time fresh apply
  #     against a full Supabase stack (auth/GoTrue/vault/pgsodium/pgvector).
  #   - .github/workflows/v2_drift_check.yml — nightly drift check against PROD.
  # See docs/superpowers/plans/2026-05-10-phase-8-v2-purge-closeout.md.

  build-and-push:
    name: Build & push image
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      image_sha: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        # 2026-05-03: lfs:false. The int8 BGE reranker was migrated from
        # Git LFS to a private HF Hub repo (Zettelkasten/bge-reranker) and
        # is pulled inside the Dockerfile builder stage. Calibration parquet
        # is still LFS-tracked but is small enough to fetch lazily on demand
        # and is not consumed during docker build.

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ops/Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/chintanmehta21/zettelkasten-kg-website:${{ github.sha }}
            ghcr.io/chintanmehta21/zettelkasten-kg-website:latest
          build-args: |
            GIT_SHA=${{ github.sha }}
            BUILD_DATE=${{ github.event.repository.updated_at }}
          # HF_TOKEN is mounted as a build-time secret in ops/Dockerfile
          # via `RUN --mount=type=secret,id=hf_token`. Build secrets do
          # NOT land in any image layer (verified by `docker history`).
          secrets: |
            "hf_token=${{ secrets.HF_TOKEN }}"
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false

  env-drift-check:
    name: Env knob drift gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify all knobs in .env.example are also in STATIC_BODY
        run: |
          EXAMPLE_KNOBS=$(grep -E '^RAG_|^GUNICORN_|^GEMINI_COOLDOWN_' ops/.env.example | cut -d= -f1 | sort -u)
          STATIC_KNOBS=$(grep -oE '"[A-Z_][A-Z0-9_]+=' .github/workflows/deploy-droplet.yml | tr -d '"=' | sort -u)
          DRIFT=$(comm -23 <(echo "$EXAMPLE_KNOBS") <(echo "$STATIC_KNOBS"))
          if [ -n "$DRIFT" ]; then
            echo "::error::Knobs documented in .env.example but missing from STATIC_BODY:"
            echo "$DRIFT"
            exit 1
          fi

  deploy:
    name: SSH deploy to droplet
    needs: [test, build-and-push, env-drift-check]
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 15
    steps:
      # Build the container .env body by iterating EVERY production
      # environment secret via toJSON(secrets). This eliminates the old
      # anti-pattern of 3 hardcoded key lists (new_envs.txt, sync script,
      # and this workflow) drifting apart — adding a new secret is now a
      # one-line edit in new_envs.txt + rerun of set_github_secrets.sh.
      #
      # BLOCKLIST: secrets consumed by the workflow itself (SSH, docker
      #            login, or GH's built-in GITHUB_TOKEN) that must NOT
      #            be written to the app's .env.
      # RENAME_MAP: GH secret name -> container env var name. Needed
      #            because GH reserves the GITHUB_ prefix, so production
      #            uses GH_*_FOR_NOTES which must remap to GITHUB_TOKEN /
      #            GITHUB_REPO at deploy time.
      - name: Checkout deploy scripts and compose files
        uses: actions/checkout@v5
        with:
          # iter-03 mem-bounded §2.10: also sync compose files so mem_limit /
          # memswap_limit edits in ops/docker-compose.{blue,green}.yml actually
          # reach the droplet. Without this, compose ceiling changes silently
          # no-op at runtime — exactly the gap that bit iter-03 mid-rollout.
          sparse-checkout: |
            ops/deploy
            ops/docker-compose.blue.yml
            ops/docker-compose.green.yml
          sparse-checkout-cone-mode: false

      - name: Sync deploy scripts to droplet
        uses: appleboy/scp-action@917f8b81dfc1ccd331fef9e2d61bdc6c8be94634 # v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_SSH_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: ${{ secrets.DROPLET_SSH_PORT }}
          source: "ops/deploy/*.sh"
          target: "/opt/zettelkasten/deploy/"
          strip_components: 2  # ops/deploy/ -> /opt/zettelkasten/deploy/

      - name: Sync compose files to droplet
        # iter-03 mem-bounded §2.10: ensure compose ceiling edits propagate.
        # Pairs with the cgroup-assertion in deploy.sh that fails the deploy if
        # the running container's memory.max doesn't match the expected value.
        uses: appleboy/scp-action@917f8b81dfc1ccd331fef9e2d61bdc6c8be94634 # v0.1.7
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_SSH_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: ${{ secrets.DROPLET_SSH_PORT }}
          source: "ops/docker-compose.blue.yml,ops/docker-compose.green.yml"
          target: "/opt/zettelkasten/compose/"
          strip_components: 1  # ops/ -> /opt/zettelkasten/compose/

      - name: Build container .env body from environment secrets
        id: env
        env:
          SECRETS_JSON: ${{ toJSON(secrets) }}
          TARGET_HOST: ${{ inputs.target_hostname || 'zettelkasten.in' }}
          DEPLOY_GIT_SHA: ${{ github.sha }}
          DEPLOY_ID: ${{ github.run_id }}-${{ github.run_attempt }}
          DEPLOY_ACTOR: ${{ github.actor }}
        run: |
          set -euo pipefail

          BLOCKLIST='["DROPLET_HOST","DROPLET_SSH_USER","DROPLET_SSH_PORT","DROPLET_SSH_KEY","GHCR_READ_PAT","GITHUB_TOKEN"]'
          RENAME_MAP='{"GH_TOKEN_FOR_NOTES":"GITHUB_TOKEN","GH_REPO_FOR_NOTES":"GITHUB_REPO"}'

          SECRETS_BODY=$(printf '%s' "$SECRETS_JSON" | jq -r \
            --argjson blocklist "$BLOCKLIST" \
            --argjson rename "$RENAME_MAP" '
            to_entries
            | map(select(.key as $k | ($blocklist | index($k)) | not))
            | map(.key = ($rename[.key] // .key))
            | map(select(.value != null and .value != ""))
            | sort_by(.key)
            | map("\(.key)=\(.value)")
            | .[]
          ')

          # REDDIT_OPTIONAL=1 opts out of the fail-fast Reddit OAuth gate in
          # website/core/settings.py:validate_reddit_credentials. Until
          # REDDIT_CLIENT_ID / REDDIT_CLIENT_SECRET are registered as GH
          # Actions secrets, this flag prevents the container from aborting
          # at startup when WEBHOOK_SECRET is set. If the static value is
          # already defined via a secret with the same name, toJSON(secrets)
          # will take precedence since STATIC_BODY is concatenated after
          # SECRETS_BODY — set the secret to override.
          # iter-03 §1C.4: ship deploy provenance through the .env file the
          # droplet sudoers ALREADY whitelists for tee. apply_migrations.py
          # picks these up from container env (--env-file). deploy.sh extracts
          # only DEPLOY_* lines (no full source — secrets stay scoped to the
          # docker --env-file path, never the host shell).
          STATIC_BODY=$(printf '%s\n' \
            "WEBHOOK_MODE=true" \
            "WEBHOOK_PORT=10000" \
            "WEBHOOK_URL=https://${TARGET_HOST}" \
            "NEXUS_ENABLED=true" \
            "REDDIT_OPTIONAL=1" \
            "RAG_FP32_VERIFY=off" \
            "RAG_DEGRADATION_LOG_DIR=/app/runtime" \
            "RAG_SMOKE_KASTEN_ID=227e0fb2-ff81-4d08-8702-76d9235564f4" \
            "RAG_STAGE2_SUB_BATCH=3" \
            "GUNICORN_MAX_REQUESTS=100" \
            "GUNICORN_MAX_REQUESTS_JITTER=25" \
            "RAG_LATENCY_SOFT_S=5" \
            "RAG_LATENCY_HARD_S=20" \
            "RAG_RETRIEVAL_LIMIT_FAST=20" \
            "RAG_RETRIEVAL_LIMIT_STRONG=25" \
            "RAG_STAGE1_SUB_BATCH=5" \
            "RAG_METADATA_EXTRACTOR_ENABLED=true" \
            "RAG_CONTEXT_FLOOR_LOOKUP=0.30" \
            "RAG_CONTEXT_FLOOR_SYNTH=0.05" \
            "RAG_CONTEXT_FLOOR_DEFAULT=0.10" \
            "RAG_CONTEXT_MIN_KEEP_LOOKUP=1" \
            "RAG_CONTEXT_MIN_KEEP_SYNTH=5" \
            "GEMINI_COOLDOWN_BASE_S=1" \
            "GEMINI_COOLDOWN_MAX_S=20" \
            "NARUTO_SMOKE_PASSWORD=${{ secrets.NARUTO_SMOKE_PASSWORD }}" \
            "SUPABASE_ANON_KEY_LEGACY_JWT=${{ secrets.SUPABASE_ANON_KEY_LEGACY_JWT }}" \
            "DEPLOY_GIT_SHA=${DEPLOY_GIT_SHA}" \
            "DEPLOY_ID=${DEPLOY_ID}" \
            "DEPLOY_ACTOR=${DEPLOY_ACTOR}" \
            "GUNICORN_WORKERS=2" \
            "GUNICORN_TIMEOUT=240" \
            "GUNICORN_GRACEFUL_TIMEOUT=90" \
            "RAG_CHUNKS_ENABLED=true" \
            "RAG_RERANK_CONCURRENCY=2" \
            "RAG_QUEUE_MAX=3" \
            "RAG_THEMATIC_MULTIQUERY_N=3" \
            "RAG_CHUNKER_SENTENCE_SNAP_ENABLED=true" \
            "RAG_CHUNK_SHARE_NORMALIZATION_ENABLED=true" \
            "RAG_CITE_HYGIENE_ENABLED=false" \
            "RAG_CITE_HYGIENE_MIN_KEEP=1" \
            "RAG_CITE_HYGIENE_FALLBACK_TOPK=3" \
            "RAG_ANCHOR_BOOST_AMOUNT=0.05" \
            "RAG_COMPARE_AWARE_ANTIMAGNET_ENABLED=true" \
            "RAG_SUPPRESS_CITATIONS_ON_REFUSAL=true" \
            "RAG_UNSUPPORTED_WITH_GOLD_SKIP_ENABLED=true" \
            "RAG_UNSUPPORTED_WITH_GOLD_SKIP_FLOOR=0.7" \
            "RAG_CHUNK_SHARE_CLASS_GATE_ENABLED=true" \
            "RAG_CHUNK_SHARE_MAGNET_RATIO=2.0" \
            "RAG_ANCHOR_SEED_FLOOR_RRF=0.30" \
            "RAG_ANCHOR_SEED_MIN_ENTITY_LENGTH=4" \
            "RAG_ANCHOR_SEED_TOP_K=3" \
            "RAG_RERANK_INPUT_FLOOR_ENABLED=true" \
            "RAG_RERANK_INPUT_FLOOR_LOOKUP=0.30" \
            "RAG_RERANK_INPUT_FLOOR_THEMATIC=0.05" \
            "RAG_RERANK_INPUT_FLOOR_DEFAULT=0.10" \
            "RAG_RERANK_INPUT_MIN_KEEP=8" \
            "RAG_SCORE_RANK_DISPROP_QUARTILES=1.0" \
            "RAG_TITLE_OVERLAP_DEMOTE_FACTOR=0.95" \
            "RAG_TITLE_OVERLAP_DEMOTE_FLOOR=0.10" \
            "RAG_DENSE_FALLBACK_ENABLED=true" \
            "RAG_SLOT_RSS_LOG_ENABLED=true" \
            "RAG_ANCHOR_BOOST_ENABLED=true" \
            "RAG_ANCHOR_SEED_INJECTION_ENABLED=true" \
            "RAG_PARTIAL_NO_RETRY_FLOOR_OFFSET_THEMATIC=-0.1" \
            "RAG_UNSUPPORTED_WITH_GOLD_SKIP_FLOOR_OFFSET_THEMATIC=-0.1" \
            "RAG_SCORE_RANK_PROTECT_ANCHORED=true" \
            "RAG_EXECUTOR_MAX_WORKERS=8" \
            "RAG_RPC_GLOBAL_SEMAPHORE=8" \
            "RAG_HTTPX_MAX_CONNECTIONS=16" \
            "RAG_HTTPX_MAX_KEEPALIVE=8" \
            "RAG_ENTITY_GATHER_SEMAPHORE=3" \
            "RAG_SCORE_RANK_GAP_BYPASS=1.5" \
            "RAG_RETRY_GAP_BYPASS=1.5" \
            "RAG_KASTEN_BOOTSTRAP_WINDOW=50" \
            "RAG_TITLE_OVERLAP_PERCENTILE=75" \
            "RAG_TITLE_OVERLAP_FLOOR_FALLBACK=0.10" \
            "RAG_ROUTER_VERSION=v4" \
            "RAG_SCORE_RANK_DEMOTE_SLOPE=0.20" \
            "RAG_ENTITY_CONFIDENCE_FLOOR=0.7" \
            "RAG_ENTITY_TOP_N=3" \
            "RAG_BLOCKLIST_MISS_THRESHOLD=2" \
            "RAG_BLOCKLIST_TTL_DAYS=7" \
            "RAG_BLOCKLIST_COLD_START_NODES=50" \
            "RAG_ANCHOR_BANDIT_ENABLED=true" \
            "RAG_ANCHOR_BANDIT_ARMS=0.25,0.30,0.35,0.40" \
            "RAG_ANCHOR_BANDIT_N_MIN=20" \
            "DB_SCHEMA_VERSION=v2")

          FULL_ENV="${SECRETS_BODY}"$'\n'"${STATIC_BODY}"

          {
            echo "env_body<<ENV_BODY_EOF"
            printf '%s\n' "$FULL_ENV"
            echo "ENV_BODY_EOF"
          } >> "$GITHUB_OUTPUT"

          secret_count=$(printf '%s\n' "$SECRETS_BODY" | grep -c '=' || true)
          echo "Built .env with ${secret_count} secret line(s) + 4 static line(s)"

      - name: Write .env, reconcile Caddy, and deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_SSH_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: ${{ secrets.DROPLET_SSH_PORT }}
          # iter-03 §1C.4: forward deploy provenance into deploy.sh so the
          # migration audit row records who/what/which-deploy applied it.
          envs: ENV_BODY,GHCR_READ_PAT,SHA
          command_timeout: 8m
          script: |
            set -euo pipefail

            # ── 1. Docker login ──
            echo "$GHCR_READ_PAT" | docker login ghcr.io -u chintanmehta21 --password-stdin

            # ── 2. Write .env (tee/chmod/chown whitelisted in sudoers) ──
            printf '%s\n' "$ENV_BODY" | sudo /usr/bin/tee /opt/zettelkasten/compose/.env > /dev/null
            sudo /usr/bin/chmod 600 /opt/zettelkasten/compose/.env
            sudo /usr/bin/chown deploy:deploy /opt/zettelkasten/compose/.env

            # iter-03 §1C.4: deploy provenance (DEPLOY_GIT_SHA / DEPLOY_ID /
            # DEPLOY_ACTOR) is included in the .env body above. apply_migrations
            # picks it up from container env via --env-file. deploy.sh extracts
            # only the DEPLOY_* lines into its own shell so the
            # ${VAR:-default} fall-throughs see the GH-supplied values without
            # full-sourcing the secrets-laden file.

            # ── 3. Ensure Caddy has --init (tini as PID 1) ──
            # Fixes "OCI runtime exec failed: procReady not received" in
            # non-root containers with cap_drop:ALL + no-new-privileges.
            # Idempotent: skips recreation if already enabled.
            # deploy user is in the docker group — no sudo needed.
            CADDY_INIT=$(docker inspect caddy --format '{{if .HostConfig.Init}}{{.HostConfig.Init}}{{else}}false{{end}}' 2>/dev/null || echo "missing")

            if [ "$CADDY_INIT" != "true" ]; then
              echo "[caddy] Recreating with --init (was: $CADDY_INIT)..."
              docker stop caddy 2>/dev/null || true
              docker rm caddy 2>/dev/null || true

              docker run -d \
                --name caddy \
                --hostname caddy \
                --init \
                --restart unless-stopped \
                --user 1000:1000 \
                --network zettelnet \
                -p 80:80 \
                -p 443:443 \
                -p 443:443/udp \
                -v /opt/zettelkasten/caddy/Caddyfile:/etc/caddy/Caddyfile:ro \
                -v /opt/zettelkasten/caddy/upstream.snippet:/etc/caddy/upstream.snippet:ro \
                -v /opt/zettelkasten/caddy/data:/data \
                -v /opt/zettelkasten/caddy/config:/config \
                -v /opt/zettelkasten/logs/caddy:/var/log/caddy \
                --memory 128m \
                --pids-limit 256 \
                --stop-timeout 10 \
                --cap-drop ALL \
                --cap-add NET_BIND_SERVICE \
                --security-opt no-new-privileges:true \
                --log-driver json-file \
                --log-opt max-size=10m \
                --log-opt max-file=3 \
                --health-cmd "wget --quiet --tries=1 --spider http://localhost:2019/config/" \
                --health-interval 30s \
                --health-timeout 5s \
                --health-start-period 15s \
                --health-retries 3 \
                caddy:2

              # Wait for Caddy to become healthy before proceeding
              for i in $(seq 1 30); do
                STATUS=$(docker inspect caddy --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}starting{{end}}' 2>/dev/null || echo "starting")
                if [ "$STATUS" = "healthy" ] || [ "$STATUS" = "running" ]; then
                  echo "[caddy] Healthy after ${i}s"
                  break
                fi
                sleep 2
              done
            else
              echo "[caddy] Already has --init, skipping"
            fi

            # ── 4. Ensure deploy scripts are executable and LF-terminated ──
            # SCP doesn't preserve +x; sed is a safety net for pre-existing files
            chmod +x /opt/zettelkasten/deploy/*.sh
            sed -i 's/\r$//' /opt/zettelkasten/deploy/*.sh

            # ── 5. Run deploy with one retry ──
            # Audit metadata (DEPLOY_GIT_SHA / DEPLOY_ID / DEPLOY_ACTOR) is
            # included in the .env body written above; deploy.sh extracts only
            # those three names from /opt/zettelkasten/compose/.env at startup
            # (no full source — secrets stay scoped to docker --env-file).
            # Invocation uses the exact NOPASSWD-allowed form.
            for attempt in 1 2; do
              if sudo /opt/zettelkasten/deploy/deploy.sh "$SHA"; then
                exit 0
              fi

              if [ "$attempt" -lt 2 ]; then
                echo "Deploy attempt ${attempt} failed; retrying once after rollback..."
                sleep 5
              fi
            done

            exit 1
        env:
          ENV_BODY: ${{ steps.env.outputs.env_body }}
          GHCR_READ_PAT: ${{ secrets.GHCR_READ_PAT }}
          SHA: ${{ github.sha }}

      - name: Verify live container env matches STATIC_BODY
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DROPLET_HOST }}
          username: ${{ secrets.DROPLET_SSH_USER }}
          key: ${{ secrets.DROPLET_SSH_KEY }}
          port: ${{ secrets.DROPLET_SSH_PORT }}
          script: |
            set -euo pipefail

            ACTIVE_COLOR=""

            # Tier 1: Try reading the state file
            if [ -f /opt/zettelkasten/deploy/active_color ]; then
              ACTIVE_COLOR=$(cat /opt/zettelkasten/deploy/active_color | tr -d '[:space:]')
            fi

            # Tier 2: Fallback to probing docker for running container
            if [ -z "$ACTIVE_COLOR" ]; then
              ACTIVE_COLOR=$(docker ps --filter "name=zettelkasten-" --format '{{.Names}}' | head -n1 | sed -E 's/^zettelkasten-//')
            fi

            # Tier 3: Verify the selected color's container actually exists and is running
            if [ -z "$ACTIVE_COLOR" ] || ! docker ps --format '{{.Names}}' | grep -qx "zettelkasten-${ACTIVE_COLOR}"; then
              echo "::warning::Active color could not be determined; skipping live-env verify."
              exit 0
            fi

            echo "Verifying against zettelkasten-${ACTIVE_COLOR}"
            docker exec "zettelkasten-${ACTIVE_COLOR}" env > /tmp/live_env.txt
            for KEY in RAG_ANCHOR_BOOST_ENABLED RAG_EXECUTOR_MAX_WORKERS RAG_RPC_GLOBAL_SEMAPHORE; do
              grep -q "^${KEY}=" /tmp/live_env.txt || { echo "::error::${KEY} missing on live droplet"; exit 1; }
            done
            echo "[smoke] env spot-check passed for zettelkasten-${ACTIVE_COLOR}"

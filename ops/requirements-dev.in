# Development & testing dependencies — NOT installed into the production image.
# Install locally with: pip install -r ops/requirements.txt -r ops/requirements-dev.txt

-r requirements.in

# Testing
pytest>=9.1.1
pytest-asyncio>=0.23
pytest-httpx>=0.30
ragas==0.4.3
deepeval==4.1.9
datasets>=5.0.1
respx>=0.23.1,<0.24
freezegun>=1.5
# asgi-lifespan: exercise FastAPI lifespan (startup/shutdown) in unit tests
# without spinning up a full TestClient. Used by JWKS pre-warm tests.
asgi-lifespan>=2.1

# Browser-driven E2E (WAVE-D Phase 1 — D-5 research recommendation).
# pytest-playwright provides native pytest fixtures; axe-playwright-python
# wraps axe-core injection for WCAG 2.2 AA audits. Chromium-only install in CI.
pytest-playwright>=0.9.0,<0.10
axe-playwright-python>=0.1.8,<0.2

# Used only in feedback module's tests — see tests/conftest.py
piexif>=1.1
